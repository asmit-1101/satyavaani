# 1) Install PyTorch first, built for your GPU. RTX 50-series cards need the CUDA 12.8 build:
#      pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#    (no NVIDIA GPU: pip install torch torchaudio)
# 2) Then: pip install -r requirements.txt

numpy
scipy
soundfile
transformers>=4.57,<4.58
gradio>=5
fastapi
uvicorn
python-multipart
audioop-lts; python_version >= "3.13"

# only for making clones (pipeline/step2_clone.py) and the LibriSpeech experiments
coqui-tts==0.27.5
torchcodec          # needs FFmpeg (shared build) on PATH
